%h2{:align => 'center'}
  = I18n.t('image_labels.edit_title')
.form-container
  = form_for [@user, @image], :html => { :role => 'form', :class => 'forms' } do |f|
    .form-group
      %label{ for: "title" }
        = I18n.t('image_labels.title')
      = f.text_field :title, class: 'form-control', value: @image.title
    .form-group
      %label{ for: "tags" }
        = I18n.t('image_labels.tags')
      = f.text_field :tags, class: 'form-control', value: @image.tags.pluck(:name).join(',')
    %button.btn.btn-success{ type: 'submit' }
      = I18n.t('image_labels.save')


:javascript
  $(function(){
    $( "#image_tags" ).tagit({
      allowSpaces: true,
      autocomplete: {
        delay: 0,
        minLength: 2,
        source: '/tags',
      }
    })
  });
